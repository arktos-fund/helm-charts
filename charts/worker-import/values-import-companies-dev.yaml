# Default values for microservice.

app:
  schedule:
    - expression: "0 3 10 * *"
      exchange: NASDAQ
    - expression: "0 3 9 * *"
      exchange: NYSE
    - expression: "0 21 20 * *"
      exchange: PA
    - expression: "1 21 * * *"
      exchange: BR
    - expression: "4 21 * * *"
      exchange: AS
    - expression: "4 28 * * *"
      exchange: LSE
    - expression: "0 10 * * *"
      exchange: LS
    - expression: "15 5 * * *"
      exchange: SHG
    - expression: "15 6 * * *"
      exchange: SHE

  image:
    repository: rg.fr-par.scw.cloud/arktos-venture/add-companies
    pullPolicy: IfNotPresent
    tag: "0.0.1"

  imagePullSecrets:
    - name: "reg-internal"
  nameOverride: ""
  fullnameOverride: ""

  serviceAccount:
    create: true
    annotations: {}
    name: ""

  podAnnotations: {}

  podSecurityContext: {}
    # fsGroup: 2000

  securityContext:
    capabilities:
      drop:
      - ALL
    readOnlyRootFilesystem: true
    runAsNonRoot: true
    runAsUser: 1000

  env: []
    # - name: API_DEBUG
    #   value: {{ .Values.app.secrets.debug }}
    # - name: API_KEY
    #   valueFrom:
    #     secretKeyRef:
    #       name: backend-user
    #       key: backend-username

  config:
    logger:
      format: fmt
      level: warn
    data:
      tdengine:
        hostname: tdengine-cluster.database
        port: 6041
        database: invest_accounts
        username: root
        password: taosdata
        timeout: 3s
        ssl: false
  services:
    exchange:
      hostname: exchanges.exchanges
      port: 11000
      timeout: 3s
    quotes:
      hostname: quotes-external
      port: 11000
      timeout: 3s

  resources:
    limits:
      cpu: 100m
      memory: 128Mi
    requests:
      cpu: 100m
      memory: 128Mi

  nodeSelector:
    k8s.scaleway.com/pool-name: app
    kubernetes.io/arch: amd64
    kubernetes.io/os: linux

  tolerations: []

  affinity: {}