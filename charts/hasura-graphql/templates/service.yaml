apiVersion: v1
kind: Service
metadata:
  name: {{ include "hasura-graphql.fullname" . }}
  labels:
    {{- include "hasura-graphql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "hasura-graphql.selectorLabels" . | nindent 4 }}
