# Default values for microservice.

app:
  image:
    repository: rg.fr-par.scw.cloud/arktos-venture/company-screeners-external

  ingress:
    enabled: false

  config:
    logger:
      format: fmt
      level: warn
    timeout: 3s
    data:
      services:
        country:
          hostname: countries-microservice.countries:11000
          port: 11000
          timeout: 3s

  resources:
    limits:
      cpu: 100m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 256Mi

  nodeSelector:
    k8s.scaleway.com/pool-name: app
    kubernetes.io/arch: amd64
    kubernetes.io/os: linux

redis:
  enabled: false

mongodb:
  enabled: false