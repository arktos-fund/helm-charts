# Default values for microservice.

app:
  image:
    repository: rg.nl-ams.scw.cloud/arktos-venture/quotes

  ingress:
    enabled: true
    hosts:
      - host: api.arktos-consulting.cloud
        paths:
          - path: /grpc/v1/quotes
            pathType: ImplementationSpecific
      - host: api.arktos-consulting.cloud
        paths:
          - path: /v1/quotes
            pathType: ImplementationSpecific

  config:
    logger:
      format: fmt
      level: warn
    timeout: 10s
    data:
      tdengine:
        hostname: tdengine-adapter.database
        port: 6041
        database: analysis
        username: root
        password: taosdata
        timeout: 3s
      services:
        companies:
          hostname: companies-microservice.companies
          port: 11000
          timeout: 3s
        countries:
          hostname: countries-microservice.countries
          port: 11000
          timeout: 3s

  resources:
    limits:
      cpu: 100m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 256Mi

  nodeSelector:
    k8s.scaleway.com/pool-name: app
    kubernetes.io/arch: amd64
    kubernetes.io/os: linux

redis:
  enabled: false

mongodb:
  enabled: false