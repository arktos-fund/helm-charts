apiVersion: v1
kind: Service
metadata:
  name: {{ include "tdengine-adapter.fullname" . }}
  labels:
    {{- include "tdengine-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tdengine-adapter.selectorLabels" . | nindent 4 }}
